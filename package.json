{
  "devDependencies": {
    "assemblyscript": "AssemblyScript/assemblyscript",
    "loop": "^3.1.3",
    "sass": "^1.18.0",
    "typescript": "^3.4.1"
  },
  "scripts": {
    "asbuild:untouched": "asc src/wasm/src/doubleArray.ts -b src/wasm/build/untouched.wasm -t src/wasm/build/untouched.wat --sourceMap --validate --debug",
    "asbuild:optimized": "asc src/wasm/src/doubleArray.ts -b src/wasm/build/optimized.wasm -t src/wasm/build/optimized.wat --sourceMap --validate --optimize",
    "asbuild:untouched2": "asc src/wasm/src/add.ts -b src/wasm/build/untouched2.wasm -t src/wasm/build/untouched2.wat --sourceMap --validate --debug",
    "asbuild:optimized2": "asc src/wasm/src/add.ts -b src/wasm/build/optimized2.wasm -t src/wasm/build/optimized2.wat --sourceMap --validate --optimize",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized && npm run asbuild:untouched2  && npm run asbuild:optimized2",
    "watch": "watch 'npm run asbuild' ./src/wasm/src",
    "start": "npm run watch & parcel index.html",
    "l": "loop asbuild:untouched"
  },
  "dependencies": {
    "watch": "^1.0.2"
  }
}
